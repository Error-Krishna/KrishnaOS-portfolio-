# This file has been superseded

This doc (`11-theme-and-polish.md`) is an earlier draft of the same Phase 7
slice now covered by `docs/11-visual-polish-and-mobile.md`. It was renamed/
superseded rather than deleted, but the old content was accidentally left
behind in full instead of being replaced with a redirect — `docs/11-phase-7-polish.md`
already pointed here as if this were a stub, which it wasn't. Fixed now.

`docs/11-visual-polish-and-mobile.md` is the canonical version. Read that
one instead — it's more complete, documents the mobile-responsiveness work
as a **deliberate deviation** from the coding prompt's original plan (see
`docs/decisions/0004-real-mobile-responsiveness.md`), and reflects the
independent-widget-dragging fix (each widget now has its own position in
`useWidgetBoardStore`, not one shared board position).

This file is kept only so old links/references don't 404 into nothing — it
carries no unique content of its own.
